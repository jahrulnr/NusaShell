Convert this experience packet into typed memory operations, per your system instructions.

Use only the packet. Do not re-read user.md or soul.md. Do not write those documents.

If nothing durable should be stored, return an empty operation list.
